# Moss Gimi 配图 QA 报告

## 批次结论

- 生成模型：`gpt-image-2-high`（依据每次 `image_generate` 返回的 `model` 字段）
- 双参考：Gimi 角色设定图 + `gimi-ai-change-chaos.png` 校准图
- 图片数量：4/4
- 比例与尺寸：全部为 1672×941，横向 16:9
- 当前状态：独立备选，尚未替换文章中的 blueprint 配图

## Shot 01

- Gimi：马帽、白毛簇、三色竖条背心、腮红、立方体吊坠、蓝裙、黑靴完整。
- 标签：`AI 理解`、`Moss 验证`、`有风险就停`、`钱包签名` 正确。
- 修正：移除卡片上模型自行添加的 `Plan`。
- 结果：PASS。

## Shot 02

- Gimi 身份锚点完整，唯一角色。
- 标签：`直接调用`、`无限授权`、`签名前验证`、`量化边界` 正确。
- 左侧断桥风险与右侧验证路径可在 3 秒内识别。
- 结果：PASS。

## Shot 03

- Gimi 身份锚点完整，唯一角色。
- 四站路径正确：交易分流 → 双 tracer → effects / expects 对账 → warning halt。
- 标签：`调用轨迹`、`状态差异`、`实际效果`、`声明边界`、`有差异就停` 正确。
- State override 回环指向交易卡，方向正确。
- 结果：PASS。

## Shot 04

- Gimi 身份锚点完整，唯一角色。
- 三步路径正确：T0 快照 → 状态变化 → 钱包复查与用户签名。
- 标签：`模拟时点`、`状态会变`、`钱包复查`、`用户签名` 正确。
- 修正：移除额外 `Plan`；用确定性字体把易混淆的 `To` 修正为明确的 `T0`。
- 结果：PASS。
